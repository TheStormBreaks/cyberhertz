// Initialize dark mode toggle
document.addEventListener('DOMContentLoaded', () => {
  // Initialize dark mode toggle
  // setupDarkMode(); // Removed import and call as per instructions

  // Any additional homepage specific functionality can be added here
});
